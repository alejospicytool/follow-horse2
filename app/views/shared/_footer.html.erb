<div class="footer navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="d-flex w-100 justify-content-around">
    <div class="d-flex align-items-center flex-column justify-content-center text-center footer-box  <%= request.path.include?("/home") ? "active" : ""%>">
      <%= link_to home_path do %>
        <%= image_tag('home.png') %>
        <p class="description">Inicio</p>
      <% end %>
    </div>
    <div class="d-flex align-items-center flex-column justify-content-center text-center footer-box <%= request.path.include?("/search") ? "active" : ""%>">
      <%= link_to search_path do %>
        <%= image_tag('search.png') %>
        <p class="description">Buscar</p>
      <% end %>
    </div>
    <div class="d-flex align-items-center flex-column justify-content-center text-center footer-box <%= request.path.include?("/add") ? "active" : ""%>">
      <%= link_to add_path do %>
        <%= image_tag('agregar.png') %>
        <p class="description">Añadir</p>
      <% end %>
    </div>
    <div class="msg-container d-flex align-items-center flex-column justify-content-center text-center footer-box <%= request.path.include?("/conversaciones/") ? "active" : ""%>">
      <% if @total_msgs > 0 %>
        <div class='indicator'>
          <div class='noti-count'><%= @total_msgs%></div>
        </div>
      <% end %>
      <%= link_to conversaciones_activas_path do %>
        <%= image_tag('mensajes.png') %>
        <p class="description">Mensajes</p>
      <% end %>
    </div>
    <div class="d-flex align-items-center flex-column justify-content-center text-center footer-box <%= request.path.include?("/perfiles") ? "active" : ""%>">
      <%= link_to profile_index_path do %>
        <%= image_tag('perfil.png') %>
        <p class="description">Mi Perfil</p>
      <% end %>
    </div>
    <div class="d-flex align-items-center flex-column justify-content-center text-center footer-box">
      <span><a href="https://api.whatsapp.com/send?phone=59894020261"><i class="fab fa-whatsapp"></i></a></span>
      <p class="description">Contactanos</p>
    </div>
  </div>

</div>

<style>

.dotted {
  border: #000000;
  border-style: dashed;
}

.fab {
  color: #ffffff;
}

.msg-container {
  position: relative;
}

.indicator {
  width: 20px;
  height: 20px;
  position: absolute;
  z-index: 1000;
  top: 0;
  right: 0;
  background-color: red;
  border: 3px solid rgb(32, 123, 110);
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
}

.noti-count {
  color: white;
  font-weight: 300;
}

.icon a {
  color: white;
}

.wrapper {
  display: inline-flex;
  list-style: none;
}

.wrapper .icon {
  position: relative;
  background: #000000;
  border-radius: 50%;
  margin: 0px 10px;
  width: 50px;
  height: 50px;
  font-size: 18px;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  box-shadow: 0 10px 10px rgba(0, 0, 0, 0.1);
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.wrapper .tooltip {
  position: absolute;
  top: 0;
  font-size: 14px;
  background: #ffffff;
  color: #ffffff;
  padding: 5px 8px;
  border-radius: 5px;
  box-shadow: 0 10px 10px rgba(0, 0, 0, 0.1);
  opacity: 0;
  pointer-events: none;
  transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.wrapper .tooltip::before {
  position: absolute;
  content: "";
  height: 8px;
  width: 8px;
  background: #ffffff;
  bottom: -3px;
  left: 50%;
  transform: translate(-50%) rotate(45deg);
  transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.wrapper .icon:hover .tooltip {
  top: -45px;
  opacity: 1;
  visibility: visible;
  pointer-events: auto;
}

.wrapper .icon:hover span,
.wrapper .icon:hover .tooltip {
  text-shadow: 0px -1px 0px rgba(0, 0, 0, 0.1);
}

.whatsapp {
  --color: #198754;
}

.icon:hover,
.icon:hover .tooltip,
.icon:hover .tooltip::before {
  background: var(--color);
  color: white;
}

@media (max-width: 425px) {
  .description {
    font-size: 10px !important;
  }
}

@media (max-width: 375px) {
  .description {
    font-size: 9px !important;
  }
}

@media (max-width: 320px) {
  .description {
    font-size: 8px !important;
  }
}

</style>